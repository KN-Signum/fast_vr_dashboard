name: vr_fast_dashboard
description: "A new Flutter project."

publish_to: "none"

version: 0.1.2+3

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  web_socket_channel: ^3.0.3
  provider: ^6.1.5+1
  web: ^1.1.1
  fl_chart: ^1.2.0
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

assets:
  - images/
